#pragma once

namespace Constants
{
    //Loop Count is set to 0, so the instance will run untill explicitly told to stop
    static const unsigned char LoopCount = 0;

    //Duration of audio buffer created in seconds
    static const unsigned char Duration = 1;
}